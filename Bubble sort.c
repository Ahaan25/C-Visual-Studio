#include<stdio.h>
int main(){
    int array[10], i, j, temp, no;
    printf("Enter number of elements in array(max 10):\n");
    scanf("%d", &no);
    printf("Enter the elements: ");
    for(i=0; i<n; i++){
        scanf("%d", array[i]);
    }
    printf("The array is: \n");
    for(i=0; i<n-1; i++){
        printf("%d\t", array[i]);
    }
    return 0;
}